import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  root: {
    background: "#030305",
  },
  toolbar: {
    display: "flex",
    justifyContent: "space-around",
  },
  iconsGroup: {
    display: "flex",
    alignItems: "center",
    columnGap: 43,
  },
});
